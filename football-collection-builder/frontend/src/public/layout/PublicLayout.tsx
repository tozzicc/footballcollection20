import { useState } from 'react'
import type { FormEvent } from 'react'
import { Link,NavLink,Outlet,useNavigate } from 'react-router-dom'
import '../styles/public-site.css'

export default function PublicLayout(){const[open,setOpen]=useState(false),[query,setQuery]=useState('');const nav=useNavigate();const submit=(e:FormEvent)=>{e.preventDefault();if(query.trim())nav(`/site/busca?q=${encodeURIComponent(query.trim())}`)};return <div className="public-site"><header className="public-header"><div className="public-header-inner"><Link className="public-wordmark" to="/site"><strong>Football Collection</strong><span>Shirt Archive</span></Link><button className="public-menu-button" aria-expanded={open} aria-controls="public-nav" onClick={()=>setOpen(x=>!x)}>Menu</button><nav id="public-nav" className={open?'is-open':''} aria-label="Navegação principal">{[['/site','Início'],['/site/paises','Países'],['/site/equipes','Equipes'],['/site/ultimas','Últimas inclusões']].map(([to,text])=><NavLink key={to} end={to==='/site'} to={to} onClick={()=>setOpen(false)}>{text}</NavLink>)}</nav><form className="public-header-search" onSubmit={submit}><label className="sr-only" htmlFor="public-search">Buscar no acervo</label><input id="public-search" value={query} onChange={e=>setQuery(e.target.value)} placeholder="Buscar no acervo"/><button>Buscar</button></form></div></header><main className="public-main"><Outlet/></main><footer className="public-footer"><div><strong>Football Collection</strong><p>Uma coleção de camisas e histórias do futebol.</p></div><p>Primeira versão visual navegável · 2026</p></footer></div>}
